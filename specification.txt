10 HTML tags used (besides html, head, body, title):
nav, header, main, section, footer, a, button, form, input, textarea

5 CSS properties used:
margin, font-family, background, color, padding

JavaScript:
Used to toggle a dark theme, show a time-based greeting, display a random quote, and show a success alert on form submit.

Bootstrap:
Used navbar, grid (row/col), cards, buttons, badges, and alerts for layout and styling.
